@using blogmongo.Models.Entiteti
@model List<BlogPost>
@{
    ViewBag.Title = "Blogs";
}

<h2>My Blogs</h2>
<div class="container">
    @foreach (BlogPost bp in Model)
    {
        <div class="border">
            <h2>@bp.Title</h2>
            <p>@bp.Description</p>
        </div>
    }
</div>

